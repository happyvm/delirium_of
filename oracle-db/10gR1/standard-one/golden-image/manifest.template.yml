# Golden image manifest TEMPLATE for Oracle Database 10g Release 1 Standard Edition One (SE1).
# create-golden-image.sh writes the real manifest; this documents the shape.
oracle_version: "10gR1"
oracle_long_name: "Oracle Database 10g Release 1"
edition: "SE1"
oracle_home: "{{ORACLE_HOME}}"
opatch_version: "{{OPATCH_VERSION}}"
created: "{{CREATED_TIMESTAMP}}"
archive: "{{ARCHIVE_NAME}}"
sha256: "{{SHA256}}"
os_source: "{{OS_SOURCE}}"
os_arch: "{{OS_ARCH}}"
applied_patches: []
