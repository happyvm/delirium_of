# Golden image manifest TEMPLATE for Oracle Database 12c Release 1 Enterprise Edition (EE).
# create-golden-image.sh writes the real manifest; this documents the shape.
oracle_version: "12cR1"
oracle_long_name: "Oracle Database 12c Release 1"
edition: "EE"
oracle_home: "{{ORACLE_HOME}}"
opatch_version: "{{OPATCH_VERSION}}"
created: "{{CREATED_TIMESTAMP}}"
archive: "{{ARCHIVE_NAME}}"
sha256: "{{SHA256}}"
os_source: "{{OS_SOURCE}}"
os_arch: "{{OS_ARCH}}"
applied_patches: []
