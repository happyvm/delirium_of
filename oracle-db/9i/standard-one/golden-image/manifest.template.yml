# Golden image manifest TEMPLATE for Oracle9i Database Standard Edition One (SE1).
# create-golden-image.sh writes the real manifest; this documents the shape.
oracle_version: "9i"
oracle_long_name: "Oracle9i Database"
edition: "SE1"
oracle_home: "{{ORACLE_HOME}}"
opatch_version: "{{OPATCH_VERSION}}"
created: "{{CREATED_TIMESTAMP}}"
archive: "{{ARCHIVE_NAME}}"
sha256: "{{SHA256}}"
os_source: "{{OS_SOURCE}}"
os_arch: "{{OS_ARCH}}"
applied_patches: []
