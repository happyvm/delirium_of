# Golden image manifest TEMPLATE for Oracle AI Database 26ai Standard Edition 2 (SE2).
# create-golden-image.sh writes the real manifest; this documents the shape.
oracle_version: "26ai"
oracle_long_name: "Oracle AI Database 26ai"
edition: "SE2"
oracle_home: "{{ORACLE_HOME}}"
opatch_version: "{{OPATCH_VERSION}}"
created: "{{CREATED_TIMESTAMP}}"
archive: "{{ARCHIVE_NAME}}"
sha256: "{{SHA256}}"
os_source: "{{OS_SOURCE}}"
os_arch: "{{OS_ARCH}}"
applied_patches: []
