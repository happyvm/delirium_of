# RHEL 7 — Hypervisor Y config notes

Store the optimised `.config` for **hypervisor Y** here, named
`config-<kernel-version>.config`.

- Derive it from `../../original/` (start from the extracted baseline).
- Document every change in `tuning-rationale.md`.
- Validate against `validation-checklist.md` before use.

> Placeholder: replace this note with the real, justified configuration.
