# rhel10/scripts/

Thin wrappers that invoke the version-agnostic scripts in
`rhel-kernel/common/` with `--rhel-major 10` pre-seeded. Run them on a
RHEL 10 host (or point the common scripts at stored artefacts).
