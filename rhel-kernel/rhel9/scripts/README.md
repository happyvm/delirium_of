# rhel9/scripts/

Thin wrappers that invoke the version-agnostic scripts in
`rhel-kernel/common/` with `--rhel-major 9` pre-seeded. Run them on a
RHEL 9 host (or point the common scripts at stored artefacts).
