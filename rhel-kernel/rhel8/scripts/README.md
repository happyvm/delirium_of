# rhel8/scripts/

Thin wrappers that invoke the version-agnostic scripts in
`rhel-kernel/common/` with `--rhel-major 8` pre-seeded. Run them on a
RHEL 8 host (or point the common scripts at stored artefacts).
